# 版本更新说明

## 未发布：主模型请求能力测试

- 新增候选回复（Swipe）：可对上一轮纯文本输入生成最多 5 个回复版本，新候选立即生效，旧版本保留在分支树中。
- 新增 `/tv swipe`、`/tv sw`、`/tv 换一个` 及 `ls/p/n/u` 简写；管理页可查看、生成和切换当前最新一轮候选。
- 候选切换会同步 AstrBot 明文历史、插件状态、预览和战役状态；开始下一轮后候选组锁定，避免回滚已经发展的剧情。
- 新增默认关闭的主模型密文测试，可选择 UTF-8 Base64、UTF-8 Hex、反转后 Base64、ROT47、Unicode 位移、XOR + Base64、Base91 或 JSON Unicode 转义。
- 开启后仅保留自动生成的明文协议说明，其余主聊天消息临时编码；模型回复由插件自动解码后再进入状态栏、归档、配图和战役状态流程。
- 修复 AstrBot 核心会话被临时密文污染：仅在 Agent 实际调用 Provider 前编码，并在核心保存历史前恢复为明文。
- 调试预览、会话备份和分支节点继续保存明文逻辑请求，并附带密文方式与实际 Provider 请求状态。
- 密文请求的字符膨胀会纳入上下文预算；该功能只用于模型能力测试，不提供安全加密。
- 新增默认关闭的主模型网址请求模式：最终明文请求发布到带 256 位随机 token 的内存临时网页，首次 Provider 请求只包含读取协议、网址和唯一的读取工具。
- 临时网页支持 HTML、JSON 内容协商和 HEAD 请求，带禁止缓存、禁止索引及严格 CSP；完成、异常、取消后立即删除，并以 TTL 清理异常残留。
- `fetch_request_url` 仅能读取当前事件对应的精确网址且每个模型流程只能调用一次；网页驾驶台支持一次工具回合，并在同一 Provider 内完成第二次生成。
- 新增 `submit_reply` 回复提交链路：模型只提交最终文本，插件将其写入当前临时页面；工具不接收网址或管理密钥，提交内容会在无害确认回合后进入状态栏、分支、配图和明文会话保存流程。
- 网址模式优先于密文模式；调试器、分支、备份和本地会话只保存明文与无 token 元数据，图片和音频仍直接附加到 Provider。
- 公网基址无效、端口占用或临时服务未启动时直接终止本轮，不会回退泄露明文。用户需自行配置域名、HTTPS、反向代理或端口映射。
- 新增 GPT Sites 托管中转后端：以 D1 短暂保存请求正文，管理接口使用独立 Bearer 密钥，公开读取 token 与请求 ID 分离且数据库只保存 token 的 SHA-256。
- 托管后端支持创建、Provider 调用前更新、立即删除、TTL 失效和本地严格工具兜底；托管鉴权、网络或写入失败时同样终止本轮，不回退泄露明文。
- 仓库新增独立 `hosted/url-request-relay` Sites 子项目，提供带 no-store、noindex、严格 CSP 的 HTML/JSON/HEAD 临时读取接口及受鉴权的状态和管理 API。
- 托管客户端使用 `curl-cffi` 的浏览器兼容 TLS 传输，避免 GPT Sites/Cloudflare 对普通 Python HTTP 指纹返回 403。
- 托管管理 API 对消息正文使用 UTF-8 Base64 传输包装，并在 Worker 入站后立即还原，降低 Cloudflare WAF 对真实角色扮演正文的 403 误判；遇到 403 时刷新浏览器传输会话并仅重试一次。
- 新增显式托管 API 代理配置，解决 AstrBot 桌面进程未继承系统代理、直连 GPT Sites 被 Cloudflare 拒绝的问题；代理仅作用于创建、更新、删除和鉴权请求。

## 0.8.2：实时游戏驾驶台与 Provider 调用修复

- “当前游戏”页移到导航顶部，接着玩驾驶台支持最新战役置顶、历史节点分批继续展开、成功提示改为自动收起的顶部 toast。
- 修复网页生成、自动摘要、长期记忆和战役状态等辅助模型调用 AstrBot Provider 时参数形态不匹配的问题，统一走 prompt + contexts/system_prompt，并阻止 messages 原样透传到 Provider，避免 OpenAI-compatible 接口报 field messages is required。
- 插件、元数据和 Web 面板版本同步提升至 0.8.2。

## 0.8.1：分支树窄屏修复

- 将分支树的会话 ID 自由输入框改为会话下拉选择器，可直接选择已发现、已绑定或战役关联的会话。
- 修复树深度按每个父子节点递增的问题；普通主线延续保持同一层级，只有同一父节点的额外子节点才增加分支缩进。
- 分支缩进设置显示上限，节点标题和 UUID 支持安全换行，避免长剧情在移动端逐渐移出屏幕。
- 新增 AstrBot conversation 边界识别：`/new` 导致 conversation ID 变化，或 `/reset` 导致上一条归档回复从当前历史消失时，会重置插件会话轮次、世界书生命周期、滚动摘要和剧情父节点。
- reset/new 只开启新的剧情根节点，不删除旧归档，也不解除独立的战役绑定与战役长期记忆。
- 分支树将每个根节点渲染为独立“会话段”，避免新话题在视觉上被误认为旧剧情的子分支。
- 分支树按各会话段的最近活动时间倒序排列；当前会话置顶展开，历史会话显示为“已归档”并默认折叠。
- WebUI“当前游戏”页新增“接着玩驾驶台”，聚合当前战役、最近剧情路线、待确认状态和“从选中节点继续”入口。
- “接着玩驾驶台”支持直接在网页输入玩家行动并调用当前 AstrBot Provider 生成回复，生成结果会保存为分支树节点，并继续复用世界书、长期记忆、战役状态和状态候选审核。
- 插件、元数据和 Web 面板版本同步提升至 `0.8.1`。

## 0.8.0：多世界战役与结构化状态

- 新增独立战役模型，可将多个 AstrBot 会话绑定到同一局 RP，同时以世界、规则集和战役作用域隔离资料与记忆。
- 自动提取的长期记忆在绑定战役后写入 `campaign` 作用域，从而真正支持同战役跨会话召回，不再默认污染其他世界。
- 新增战役权威状态、可配置字段说明和规则提示，固定注入当前请求，适合维护位置、时间、物品、伤势、任务、线索与人物关系。
- 每轮回复完成后可调用 LLM 提出最小 JSON 状态补丁；默认进入待确认审核箱，也可由单个战役选择自动应用。
- 状态补丁支持 set、delete、increment、append 和 remove，并保存变更前后快照、来源轮次、理由和审核状态。
- 剧情分支快照加入战役状态；从旧节点继续时恢复对应时间线，并优先采用该节点已经审核应用的状态。
- WebUI 新增“战役状态”页面，提供战役编辑、会话绑定、JSON 状态维护和候选变更审核。
- 数据库 schema 升级至 9，插件、元数据和 Web 面板版本同步提升至 `0.8.0`。

## 0.7.5：会话命令与摘要边界修复

- 版本号提升至 `0.7.5`，同步更新插件元数据、README 徽章和 Web 面板包版本。
- 新增 `/tv` 作为 `/tavern` 的完整短别名，并兼容 QQ `@机器人 /tv ...` 与 `@机器人 /tavern ...` 的未剥离命令格式。
- 新增 `/tavern undo`（兼容 `rollback`、`撤回`）命令，用于删除最近一次用户消息及对应助手回复，方便重新生成不满意的剧情。
- 撤回时同步回退插件剧情状态、滚动摘要、请求预览和该轮自动提取的长期记忆，同时保留分支树快照用于恢复。
- 修复 AstrBot `/reset` 后旧会话自动摘要仍可能注入新会话的问题；当已保存摘要的 `covered_until` 边界不属于当前历史时，自动清除该摘要状态。
- 补充回归测试，覆盖 `/tv` 别名、`undo` 回滚和 reset 后摘要边界失效场景。

## 0.7.2：历史消息净化

- 剥离 assistant 历史消息中的 `thinking`、`reasoning`、`reasoning_content` 及 `think` / `reasoning` message parts，避免它们混入真实请求的最终 `messages[]`。
- 为真实请求、只读模拟和分支树回放统一增加历史消息归一化逻辑，只向构建器传入 `role + 纯文本 content`。
- 补充回归测试，覆盖 AstrBot `bot/message` 结构、`think` 片段和 reasoning 字段共存时的上下文净化行为。

## 0.7.1：快捷回复体验补全

- 将 WebUI 中的 `Quick Replies` 页签和相关操作文案统一调整为中文标签，减少术语混用。
- 新增默认“快捷回复”预设，包含继续剧情、丰富描写、代写回复、润色重写、总结剧情和保持角色等常用项。
- 新建快捷回复文档时默认带出常用项，同时在后端首次初始化时自动创建全局默认预设。
- 补充快捷回复页内使用说明、生成模式解释和 `/tavern qr` 命令示例，开箱即可参考使用。

## 0.7.0：管理页体验改版

- 重构 WebUI 导航与信息架构，新增分组侧边栏和配置向导首页，降低初次配置成本。
- 绑定管理改为“当前目标有效配置”视图，优先展示最终生效的角色、预设、世界书与素材。
- 调试器新增本轮结论摘要，快速显示预设、角色、世界书、素材、记忆、摘要和警告状态。
- 世界书与创作素材编辑改为摘要卡片列表、搜索筛选和展开编辑，适配更长素材库。
- 长期记忆页面改为按作用域分组的卡片列表，强化状态、分类与操作反馈。
- 移除剩余原生 `confirm()` 弹窗，统一为页面内二次确认，避免 AstrBot 页面环境无反应。
- 统一新增保存、导入、删除和检索操作的提示反馈，提升页面可理解性。

## 0.6.5：分支树与存档归档

- 新增分支树归档表 `story_nodes`，真实请求完成后保存最终 `messages[]`、请求预览、绑定快照、检索命中、记忆命中、会话状态和 assistant 回复。
- 新增当前节点状态，后续请求会以最近节点为父节点保存；从旧节点继续时会创建新的分支节点。
- 新增 `/tavern archive list|show|branch` 和 `/tavern branch` 命令，用于查看归档节点或从指定节点继续。
- WebUI 新增“分支树”页面，支持查看节点树、重命名节点、查看快照详情、从节点继续和导出归档 JSON。
- 会话备份 ZIP 新增 `story-nodes.json`；`/tavern reset` 保持原语义，不删除分支树归档。
- 合并 0.6.1 角色组与角色切换、0.6.2 长期记忆治理增强。

## 0.6.2：长期记忆治理增强

- 长期记忆新增 `pending`、`active`、`archived`、`rejected` 状态，自动提取可配置为待确认后再注入。
- 新增长期记忆重要度、来源类型、来源引用、过期时间、内容哈希和 embedding 模型记录。
- 长期记忆检索只注入 active 且未过期的记忆，并让重要度参与排序。
- 新增同作用域、同分类、同内容的精确去重，避免手动保存或自动提取反复写入重复记忆。
- WebUI 长期记忆页新增状态筛选、勾选、多选确认、拒绝和归档操作。
- 调试器新增长期记忆注入面板，显示本轮 query、作用域、注入条数、命中分数、重要度、状态和来源。

## 0.6.1：角色组与角色切换

- 新增角色组管理，支持 WebUI 创建角色组、选择成员、排序成员和配置切换策略。
- 角色组支持 `round_robin` 自动轮询与 `manual` 手动策略，用户消息点名成员角色时本轮优先切换。
- 新增 `/tavern character status|next|use <角色名>` 命令，用于查看、推进和锁定当前会话角色。
- 调试器新增本轮实际选中角色、选择原因、角色组成员和切换状态显示。
- 补齐 `character_group` 文档默认结构、校验和只读模拟路径，确保调试器与真实请求使用同一选择逻辑。

## 0.6.0：混合检索与创作素材增强

- 新增世界书与创作素材混合检索，支持关键词召回、向量召回和加权合并。
- 新增创作素材知识库索引，支持 SQLite 素材导入、分类去重、命中统计和检索日志。
- 管理台新增“创作素材”入口、SQLite 素材导入、检索测试与统计面板。
- 向量 embedding 按内容哈希持久化，重建索引时复用已有向量，并按 embedding provider 隔离内存缓存。
- 修复 keyword 模式不应依赖向量开关、生成模式被检索模式覆盖、禁用素材进入有效索引、素材默认注入位置和删除确认无响应等问题。

## 0.5.0：长期记忆与运行仪表盘

- 新增长期记忆持久化，支持自动提取、手动新增、禁用、删除和向量检索注入。
- 新增运行仪表盘，记录真实请求的估算 Token、耗时、世界书命中、摘要状态、记忆命中和 Provider 调用量。
- 新增 `memories` 与 `runtime_metrics` SQLite 表，并加入独立保留期清理。
- 管理台新增“长期记忆”和“运行仪表盘”页面。
- 记录分支/存档设计前提：完整回溯需要插件主动保存 messages 快照，0.5.0 暂不实现完整分支树。

## 0.4.1：资料与会话备份导出

- 角色卡、提示词预设、世界书和用户设定支持分别导出 JSON。
- 支持按资料类别导出 ZIP，以及一键导出全部资料 ZIP。
- 调试器支持导出当前最终 `messages[]` 为纯 JSON。
- 新增重置前会话备份 ZIP，保存 `messages[]`、完整请求预览和插件会话状态。
- ZIP 内附清单并按资料类型分目录，导出文件名自动规避 Windows 非法字符。

## 0.4.0：自动摘要与会话生命周期

- 新增会话级滚动摘要，支持独立 Provider、触发阈值、自定义提示词、输出上限和超时。
- 历史压缩后保留近期消息，并将生成内容与静态 Summary 合并注入。
- 摘要失败自动降级且不推进进度；只读模拟不调用模型、不修改状态。
- 调试器新增摘要来源、覆盖数量、Provider、触发状态和失败原因。
- `sessions` 与 `previews` 新增更新时间索引及独立 TTL 自动清理。
- README 重构为项目主页，全部版本记录迁移至本文件。

## 0.3.10：统一请求准备与运行时常量

- 真实生成与只读模拟共用资料收集和结果序列化。
- 模拟补齐创作素材加载，与真实请求保持一致。
- 集中管理插件 ID、显示名、版本和 API 前缀。

## 0.3.9：配置页分组

- Dashboard 配置改为深色层次分组。
- 兼容旧版平铺配置并保留已有值。

## 0.3.8：限制历史消息条数

- 新增 `history_max_messages`，可只发送最近 N 条历史。
- 调试器标记由条数限制移除的旧消息。

## 0.3.7：调整上下文裁剪顺序

- 优先裁剪较旧历史并保护最近消息。
- 角色卡、Persona、摘要、记忆和 PHI 改为核心块。
- 新增历史优先裁剪和近期保留配置。

## 0.3.6：修复大型预览发送

- `/tavern preview` 使用分包转发及普通消息降级。
- 修复调试器会话 ID 重复编码。

## 0.3.5：修复本地命令误触发模型

- `/tavern preview|status|reset` 返回后不再继续调用 LLM。
- 未知子命令只显示用法。

## 0.3.4：增强 QQ 合并转发

- 长回复拆为小节点和多个转发包。
- 失败时仅降级未发送部分，避免丢失或重复。
- 新增每包节点数和发送间隔配置。

## 0.3.3：修复并发状态与配图顺序

- Web 特殊生成、重置和命令统一使用会话锁。
- 自动配图延后至正文发送完成。
- 修复调试 Persona、仓库地址及 data URL 校验问题。

## 0.3.2：性能与并发优化

- 优化中日韩 token 估算。
- 向量 embedding 增加 LRU 缓存及错误降级。
- SQLite IO 异步化并增加会话级并发锁。
- 自动配图增加并发限制。

## 0.3.1：改进会话选择

- 绑定和调试会话改为可输入搜索框。
- 补齐版本标注。

## 0.3.0：新增自动配图

- 支持在模型回复后异步生成并补发图片。
- 生图能力为软依赖，异常不会阻断文本回复。
- 增加模式、尺寸、额度及提示词配置。

## 0.2.6：支持纯文本提示词导入

- 支持 `.txt` 和 `.md` 提示词预设。
- 自动处理首尾单层引号并保留默认提示块顺序。

## 0.2.5：增强普通消息分片重试

- 增加逐片日志、失败重试和等待配置。
- 分片最终失败时停止后续发送，避免重复。

## 0.2.4：新增 QQ 普通消息分片

- 长回复可拆为多条普通 QQ 消息。
- 支持每条最大字符数和发送间隔。

## 0.2.3：新增 QQ 合并转发分片

- 支持触发字符数和 Node 最大字符数。
- 优先按段落、换行及句末安全切分。

## 0.2.2：修复调试会话来源

- 从绑定记录生成会话选项。
- 更新页面缓存版本。

## 0.2.1：完善绑定会话兜底

- 会话目录为空时使用绑定记录。
- 自动选择有效绑定并显示实际配置。

## 0.2.0：完成核心角色扮演工作流

- 增加中文向导、资料编辑器、绑定管理和调试器。
- 增加 Persona/会话目录、文档校验和未知字段往返保留。
- 修复未绑定资料隐式生效并增加角色卡覆盖策略。

## 0.1.0：初始版本

- 支持角色卡、角色组、预设、世界书和创作素材。
- 支持递归扫描、生命周期、概率、深度注入及请求预览。
- 提供 AstrBot Dashboard 管理页面。
